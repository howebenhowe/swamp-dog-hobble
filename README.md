# swamp dog hobble

vitepress based website for the Brooklyn-based Swamp Dog Hobble intentional community
deployed to github pages

to run dev environment:

1. install pnpm
1. at project root, run `pnpm install`
1. at project roon, run `pnpm swampdog:dev`
1. dev server should be visible at http://localhost:5173
